account_lists:
  - name: "teortaxestex_test"
    accounts: ["teortaxestex"]

# Global configuration
window_hours: 8       # lookback window for new tweets (24 * 3 days = 72 hours)
max_per_account: 250   # cap to prevent spam
